#!/bin/sh
set -eu

if [ "$#" -ne 2 ]; then
    printf 'usage: %s MOUNT_POINT DEVICE_ID\n' "$0" >&2
    exit 2
fi

mount_point=$1
device_id=$2

printf 'archive %s from %s\n' "$device_id" "$mount_point"
