[build-system]
requires = ["hatchling>=1.25.0"]
build-backend = "hatchling.build"

[project]
name = "jeb-protocol"
version = "0.1.0"
description = "Shared contracts for Jeb servers and clients."
requires-python = ">=3.12"

[tool.hatch.build.targets.wheel]
packages = ["src/jeb_protocol"]
