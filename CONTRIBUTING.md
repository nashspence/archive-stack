# Contributing

Keep changes within the component boundaries documented in `AGENTS.md`, and run the focused
checks plus the repository validation commands before submitting them.

Contributions are submitted under the existing license assigned to the files or component
being changed. New files use the license assigned to their containing component. See
`LICENSE.md` and the machine-readable path assignments in `REUSE.toml`.
