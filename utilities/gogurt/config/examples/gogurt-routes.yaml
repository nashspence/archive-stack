schema_version: 1
kind: gogurt.routes
routes:
  example-camera-card:
    command:
      - "{python}"
      - "{config_dir}/scripts/fake_archive_device.py"
      - "{mount_point}"
      - example-camera
  disabled-example:
    enabled: false
    command:
      - "{python}"
      - "{config_dir}/scripts/fake_archive_device.py"
      - "{mount_point}"
      - disabled
